export default function QualityPracticesLayout({ children }) {
  return (
    <div className="quality-practices-layout">
      {/* Quality practices specific content */}
      {children}
    </div>
  );
}