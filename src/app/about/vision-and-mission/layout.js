export default function VisionAndMissionLayout({ children }) {
  return (
    <div className="about-layout-specific-styles">
      {/* Your about page specific layout components */}
      {children}
    </div>
  );
}