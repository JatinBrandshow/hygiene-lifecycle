export default function AccreditationLayout({ children }) {
  return (
    <div className="accreditation-layout">
      {/* Your accreditation-specific layout components */}
      {children}
    </div>
  );
}